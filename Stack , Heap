当创建线程时，操作系统为每一个系统级的线程分配栈。 操作系统通过调用语言的运行为应用程序分配堆；
栈附属于线程，因此当线程结束栈被回收。堆通常通过运行时在应哟程序启东时被分配，当应用程序（进程）退出时被回收；
栈比堆快； 当线程创建时，设置栈的大小，当应用程序启动时，设置堆的大小；
栈： 
  存储局部数据，返回地址，用做参数传递，
  相比堆而言在栈上分配要快的多
  栈过多时，可能导致栈溢出， （无穷的递归调用，大量的内存分配）
  
堆：
  堆上的变量必须手动释放，，= 数据可用 delete, delete[] 或者free来释放
  
